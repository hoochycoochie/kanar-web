import React from "react";
import Profile from "../common/Profile";
import Layout from "../common/layout";

const ProfileSaleManager = () => {
  return (
    <Layout module="salerManager">
      <Profile />
    </Layout>
  );
};

export default ProfileSaleManager;
