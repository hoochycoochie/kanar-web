import React from "react";
import Layout from "../common/layout";

const SalePointList = () => {
  return (
    <Layout module="salerManager">
      <div>SalePointList</div>
    </Layout>
  );
};

export default SalePointList;
