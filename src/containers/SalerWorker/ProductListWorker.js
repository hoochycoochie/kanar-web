import React from "react";
import Layout from "../common/layout";

const ProductListWorker = () => {
  return (
    <Layout module="salerWorker">
      <div>ProductListWorker</div>
    </Layout>
  );
};

export default ProductListWorker;
