import React from "react";
import Layout from "../common/layout";

const SalePointListWorker = () => {
  return (
    <Layout module="salerWorker">
      <div>ProductOperationListWorker</div>
    </Layout>
  );
};

export default SalePointListWorker;
